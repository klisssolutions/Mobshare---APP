<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/azulPadrao</item>
        <item name="colorPrimaryDark">@color/azulEscuro</item>
        <item name="colorAccent">@color/branco</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">#d9d9d9</item>
        <item name="colorControlActivated">#ffea00</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="StyledTilEditTextTheme">
        <item name="colorControlNormal">#c89f9f9f</item> // Cor padrão da linha
        <item name="colorControlActivated">@color/azulPadrao</item> // Cor linha e texto quando recebe foco
    </style>

    <style name="StyledTilEditText">
        <item name="android:theme">@style/StyledTilEditTextTheme</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:textColorHint">#c89f9f9f</item> // Cor texto(hint) sem foco
    </style>
</resources>
